---
name: fix-article
description: Точечная правка готовой статьи. Аргументы: NNN (номер статьи), описание правки.
---

# fix-article

Точечная правка готовой статьи. Использует `article-fixer`.

## Аргументы

```
/fix-article <NNN> "<описание правки>"
```

- `NNN` — номер темы (префикс папки) или полный id папки (`005-slug-dko`), если под темой несколько статей.
- `<описание>` — что поправить.

## Алгоритм

### 0. Проверка: мы в worktree?

```bash
GIT_DIR=$(git rev-parse --git-dir)
COMMON_DIR=$(git rev-parse --git-common-dir)
```

Если `GIT_DIR == COMMON_DIR` — мы в main. Предупредить, но не блокировать:
> «⚠️ Правка статьи в main-сессии. Изменения уйдут в основную папку сразу. Для многозадачности рекомендую открыть worktree-сессию.»

### 1. Найти папку

Резолвить папку детерминированно (NNN после точки 2 не уникален - у темы бывает несколько статей):
```
.claude\scripts\_node.cmd .claude\scripts\resolve-article-dir.mjs articles <NNN>
```
- `found == false` → сообщить «Статья `<NNN>` не найдена» и стоп.
- `ambiguous == true` → показать `candidates` (имя папки / жанр / площадка / state), попросить уточнить полный id (напр. `005-ukladka-plitki-dko`). НЕ брать первую молча.
- иначе `article_dir = <ответ>.dir`.

После нахождения — записать `.claude/tmp/current-task.txt` с путём к этой папке. Это нужно, чтобы pre-commit hook разрешил коммит правок (в worktree-режиме).

### 2. Определить целевой файл

По описанию правки и состоянию папки:
- Если есть `output.html` — обычно правим его.
- Если есть `tilda/head.html` или `tilda/t123.html` — спросить пользователя или применить правку к `output.html`, после чего перезапустить `tilda-split.mjs`.
- Если есть только `article.md` (статья ещё не собрана) — править его.

Если непонятно — спросить пользователя.

### 3. Делегировать `article-fixer`

Маркер: маркер на этот раз необязателен — `article-fixer` возвращает дифф, не новый файл.

Промт:
```
article_dir: <abs path>
description: <описание правки>
project_root: <...>
```

### 4. Если правили `output.html` и есть Тильда

Прочитать `ЗАКАЗЧИК.md`. Если Платформа = «Тильда» И существует `<dir>/tilda/`:
```
.claude\scripts\_node.cmd .claude\scripts\tilda-split.mjs <dir>
```

Сообщить:
> «Тильда-блоки переразделены: tilda/head.html и tilda/t123.html обновлены.»

### 5. Коммит и завершение

После применения правки:
```
git add <task-dir>/...
git commit -m "Fix article <NNN>: <короткое описание правки>"
```

(pre-commit hook проверит, что мы трогаем только `task-dir` или `.claude/tmp/` — если правка случайно затронула общий файл, он откажет.)

Вывести дифф (уже выведен субагентом) + указать, какие файлы изменены.

**Если ты в worktree:** напомнить пользователю:
> «⚠️ Запусти /handoff перед закрытием сессии — иначе правка останется в worktree.»

## Запреты

- Не запускай весь цикл `seo-statya` заново.
- Не правь `meta.json` — состояние не меняется.
- Не используй длинное тире (—) и среднее (–). Только дефис (-).
