# v8 - карта этапов 1, 2 и 3

v8 - это новый набор файлов **рядом** со старым конвейером, а не его переделка. Ни один файл
v7 не переписан по существу. Этап 1 закрывает предпроектный анализ: от вводной до двух
документов заказчику и согласованного контракта данных. Этап 2 закрывает слой письма: от
контракта до одного собранного `site.html`. Этап 3 закрывает режим магазина: от прайса до
пакета ТЗ разработчику и товарного csv.

Два скила, не три и не один:

- **`/site analiz`** - предпроектный анализ. Собран на этапе 1.
- **`/site proto`** - тексты и прототип: лендинг, главная, услуги, инфо-страницы, дальше
  категории и товары. Один скил на все, отдельного скила под каталог нет.
- **`/seo-faq`** остается как есть и в v8 не переписывается.

## ГРАНИЦА ТЕКУЩЕГО СРЕЗА

Каталог и товары закрыты этапом 3, и тракт письма ДОВЕДЕН до конца. Типы
**`category`, `facet`, `product`** типизируются из `catalog.json` (`catalog.mjs`,
`render-products.mjs`, `build-catalog-tz.mjs`) и дальше идут обычным путем: `plan.mjs`,
`build-tasks.mjs`, `site-author`. Вилки объема у них КОРОТКИЕ (категория 4-6, посадка 3-5,
карточка 5-8): центр тяжести категории не текст. Блоки каталога стоят только каталожным
страницам - `CATALOG_ONLY` применяется к типам, которые из каталога не пришли. Узел
`service_like` идет тем же трактом с признаком `from_catalog` и вилкой 4-6 блоков.
Третьего движка письма в v8 нет, автор один.

Считается объем ДВУМЯ числами: посадок в контракте (столько адресов и метатегов верстает
разработчик) и из них индексируемых (столько страниц получают интро и текст под листингом).
Закрытая `canonical` комбинация - склейка, и текста ей не пишут.

**Новая граница одна: структура сайта на новый контракт НЕ переведена.** `/seo-struktura`
живет машинерией v7 со своим `master_list`, и режим магазина дерево оттуда не берет -
источник узла помечается `price`, `brief` либо `competitors`. Перевод структуры на контракт
v8 это отдельная работа по решению владельца, и до нее два дерева живут рядом, не сверяясь.
Заглушки вместо перевода нет намеренно: половина перевода, которая «как бы работает»,
дороже честно объявленной границы.

## Что создано на этапе 1

| Файл | Что делает |
|---|---|
| `.claude/skills/site-analiz/SKILL.md` | Шесть шагов скила: три вопроса оператору, фактура, смыслы и разведка, сборка контракта, два документа, гейт ответов. Состояние выводится из содержимого папки, списка состояний нет |
| `.claude/skills/site-analiz/project.schema.json` | Контракт `project.json` - единственный машинный вход `/site proto`. Схема закрыта: `additionalProperties false` везде, поэтому поле-обоснование в нее физически не влезает. Минимумов на количество в ней нет: недобор дает вопрос заказчику, а не команду выдумывать |
| `.claude/skills/site-analiz/doc1.tmpl.html` | Шаблон документа 1 «Что мы поняли о вашем бизнесе». Ветки «иначе» в шаблонизаторе нет намеренно, и дефолта нет в самом сборщике: причина без доказательства выбрасывается, а не подписывается словами |
| `.claude/skills/site-analiz/doc2.tmpl.html` | Шаблон документа 2 «Что нам от вас нужно»: одна страница, не больше 10 вопросов, порядок машинный |
| `.claude/skills/site-proto/pages.yml` | Ядро состава страниц: 30 блоков, 9 признаков ниши, 5 значений NEEDS, стартовые составы по семи типам страниц, включая собственный состав лендинга. Читают и `/site proto`, и валидатор |
| `.claude/agents/site-intake.md` | Фактура: бриф, созвон, документы, старый сайт. Пустой вход - штатный режим. Маска ИНН и ОГРН. У каждой строки источник, у каждой дырки - вопрос в `gaps[]` |
| `.claude/agents/site-market.md` | Смыслы и разведка: сводка аудитории, оффер, лексикон, замер лидеров. Думает пятью развернутыми портретами и записывает только сводку |
| `.claude/scripts/site/_contract.mjs` | Общие правила контракта на все скрипты: проверяемый факт, маркер снятия, разбор `pages.yml`, обход схемы, бюджет, типографика, список смысловых решений гейта. Двух определений одного правила в этапе нет |
| `.claude/scripts/site/queue.mjs` | Раскладка `sites/NNN-slug/`, следующий шаг из содержимого, единый журнал исключений с обязательным основанием, отметка гейта |
| `.claude/scripts/site/build-project.mjs` | Склейка `parts/` в контракт по белому списку полей: считает ворота, признаки ниши, `facts[].q`, `directions[].serves`, вес вопросов. Перед записью прогоняет собранное через ту же схему, что и валидатор |
| `.claude/scripts/site/verify-data.mjs` | Валидатор контракта: схема, бюджет знаков, запрет полей-обоснований, ссылки на `pages.yml`, засев `publish`, типографика, пересчет ворот |
| `.claude/scripts/site/build-doc.mjs` | Сборка обоих документов подстановкой в `docs/understood.html` и `docs/ask.html`. Прозу не генерирует никто: нет данных - секция не печатается |
| `.claude/scripts/site/apply-answers.mjs` | Гейт: восемь смысловых решений, каждое ровно в одно поле. Записи без дифф-листа не бывает |
| `.claude/tests/site/run.mjs` | 75 шагов: потолки в знаках, бюджеты количества, валидатор на фикстурах, ворота, оба документа, сквозной прогон от init до гейта, типографика, инвариант запретов |
| `docs/v8/rubric.md` | Прибор качества текста: 7 критериев с весами и процедура слепой сверки с архивом |
| `docs/v8/trace.csv` | Трассировка: куда уехало каждое правило v7 и почему снято то, что снято |

## Что создано на этапе 2: слой письма

Двенадцать шагов `/site proto`: адреса - замер рынка - разметка блоков - план - задания -
эталонная страница - гейт манеры - веер остальных - проверка - усиление - судья - сборка.

| Файл | Что делает |
|---|---|
| `.claude/skills/site-proto/SKILL.md` | Манифест скила: двенадцать шагов, состояние из содержимого папки, три исхода пустой фактуры, граница среза (каталог и товары - следующий) |
| `.claude/skills/site-proto/AUTHOR.md` | Методичка автора: как пишется раздел, формула выгоды, ответ без числа, первый экран, надписи кнопок, ТРИ полных образца блока. Приемов и образцов в ней больше, чем запретов, - это инвариант, а не пожелание |
| `.claude/skills/site-proto/rules.yml` | Реестр правил проверки страницы: блокирующие и предупреждающие, по 12 максимум. Список живет ЗДЕСЬ, в коде его нет; правило без проверки и проверка без правила валят прогон |
| `.claude/agents/leader-mapper.md` | Снимает страницы лидеров и подтверждает разметку блоков. Имена блоков только из `pages.yml` либо `other:<строка>` |
| `.claude/agents/site-author.md` | ЕДИНСТВЕННЫЙ пишущий агент: страница целиком, один проход, один файл. Ни `block-planner`, ни `slot-mapper`, ни `blueprint` не существует |
| `.claude/agents/site-strengthener.md` | Симметрия качества: роль БЕЗ операции «резать». Четыре действия, все на добавление, потолок +10% к объему. Зовется только при непустом списке `lift.json` |
| `.claude/agents/site-judge.md` | Вердикт по `docs/v8/rubric.md`, постранично. Судит и НЕ чинит: иначе оценка подстраивается под свою же правку |
| `.claude/agents/site-editor.md` | Правит ТОЛЬКО адресные нарушения из вывода верификатора. Неназванное не трогает никто |
| `.claude/scripts/site/pages.mjs` | Адреса и слаги из контракта. Существующий адрес не пересоздается, смена адреса дает строку в `redirects.csv`, каннибализация ловится ДО письма |
| `.claude/scripts/site/recon.mjs` | Замер рынка: что снять и как разобрать снятое. Глубина от `business.site_kind`, при `tier: basic` платные инструменты не вызываются вовсе. Сам в сеть не ходит |
| `.claude/scripts/site/map-blocks.mjs` | Разметка страницы лидера по DOM-приметам, словарь заголовков - ВТОРОЙ сигнал. Признак живости: от 3 заголовков и 800 знаков из `main` за вычетом обвязки |
| `.claude/scripts/site/plan.mjs` | Состав страницы арифметикой: ШЕСТЬ правил R1-R6 и ни одного седьмого. Первичная единица объема - ЧИСЛО БЛОКОВ, знаки вторичны. Седьмое правило валит сборку на импорте |
| `.claude/scripts/site/build-tasks.mjs` | Задание автору, метатеги и `theses.md`. Два потолка входа считаются РАЗДЕЛЬНО, и машинный инвариант один: материала больше, чем инструкций |
| `.claude/scripts/site/verify-page.mjs` | Исполняет реестр `rules.yml` по написанной странице. Все, что ловится грепом, живет тут, а не в промте автора. Служебный хвост вырезается ДО всех проверок |
| `.claude/scripts/site/lift.mjs` | Адресный список усиления по МАРКЕРАМ, 0 токенов. Пустой список - нормальный исход и агента не зовет. Предложить сокращение он не умеет ни одной строкой |
| `.claude/scripts/site/assemble.mjs` | Сборка страниц в ОДИН self-contained `site.html`: тема wireframe, hash-роутер, позиционный фолбэк для раздела без метки. Служебный хвост в верстку не попадает |
| `.claude/scripts/site/verify-build.mjs` | Последний рубеж: блоки плана, ядовитые числа лидеров, маркеры, повторы id, типографика, хвост |
| `.claude/tests/proto/run.mjs` | 60 шагов: потолки в знаках, ДВЕ мерки пропорции, инвариант образцов (и проверка самого счетчика), бюджеты количества, детерминизм плана, шесть правил на фикстурах, замер, проверка страницы, усиление и отметки лестницы, пустая фактура, метатеги от формулы до сборки, сквозной прогон лендинга |
| `docs/v8/contracts-proto.md` | Форматы слоя письма: `pages.json`, `leaders/<cluster>.json`, `plan.json`, `tasks/<slug>.md` и `tasks/meta.json`, `pages/<slug>.md`, `theses.md`, `verify.json` и `lift.json` |

## Что создано на этапе 3: режим магазина

Включается одним переключателем `business.type` (`shop` либо `both`) и начинается не с
письма, а с ПРИЕМКИ ДАННЫХ: ТЗ каталога без товарных данных описывает фильтры, которые
нельзя построить. Объем работ считается в ПОСАДКАХ, а не в категориях.

| Файл | Что делает |
|---|---|
| `.claude/skills/site-proto/CATALOG.md` | Справочник каталога, подгружается ТОЧЕЧНО по `business.type`. Семь шагов режима, таблица «вердикт - что делаем и чего не обещаем», нормы текста категории, приоритет внутри карточки с описанием ПОСЛЕДНИМ, тиры и два образца |
| `.claude/skills/site-proto/catalog.schema.json` | Контракт `catalog.json`: дерево с тремя падежными формами, оси, посадки, `signals`, тиры и ДВА списка блокировок. `additionalProperties false` везде |
| `.claude/agents/catalog-architect.md` | Восьмой и последний агент конвейера. Пишет ПРАВИЛА: падежи, одну формулу метатегов на весь каталог плюс исключения, оси клиентским языком, шаблоны товарного текста. Данные считают скрипты |
| `.claude/scripts/site/catalog.mjs` | `ingest` (прайс - машинный вердикт готовности данных), `tree` (дерево, оси по порогу 0,7, посадки, обе блокировки), `mode` (четыре числа и режим узла). В сеть не ходит |
| `.claude/scripts/site/verify-catalog.mjs` | Гейт контракта: схема плюс шесть проверок, которых схема не умеет, и пересчет DERIVED. Именно `counts.landings` едет в смету |
| `.claude/scripts/site/render-products.mjs` | Товарный конвейер: csv на входе, csv на выходе, ноль токенов на карточку тира B. Правило пропуска, тиринг, контроль уникальности, ОДИН инклуд доставки на сайт |
| `.claude/scripts/site/build-catalog-tz.mjs` | Пакет разработчику подстановкой: `TZ-katalog.md`, `meta.csv` значениями, `facet-landings.csv`, `robots.snippet.txt`. Прозу не генерирует никто, доля табличных строк считается числом |
| `.claude/tests/catalog/run.mjs` | 72 шага: четыре вердикта на четырех прайсах, порог оси и обе блокировки, три падежные формы, четыре условия режима с вето и `thin_category`, типизация страниц, правило пропуска, тиринг, уникальность, метатеги, пакет ТЗ, сквозной прогон магазина, бюджеты и страховка |
| `docs/v8/contracts-catalog.md` | Форматы режима: прайс на входе, `attr_map.json`, четыре объема режима, анкета платформы из девяти ответов, `catalog.json` полями на примере |

## Что держит качество

- **Бюджеты количества.** Блоков не больше 30, признаков ниши ровно 9, значений NEEDS ровно 5,
  скриптов не больше 20, агентов `site-*` не больше 8. Новое добавляется только вместо
  старого, счетчик валит сборку.
- **Пустая фактура - законное состояние.** Плейсхолдера нет: блок без факта снимается или
  идет по своему FALLBACK, а вопрос уходит заказчику с весом, равным числу ждущих блоков.
  Минимумов на количество фактов и направлений в схеме нет намеренно: 15 придуманных строк
  хуже 9 настоящих.
- **Источник у каждого проверяемого утверждения.** Нет источника - предложение не пишется.
  Факт, выросший из ответа заказчика, подписан источником `ответ`, а не брифом, которого
  могло не быть.
- **Промт первичен, внешние данные вторичны.** Портреты и возражения рождаются рассуждением
  агента. Парсинг форумов - право, а не обязанность. Ярлык `persona` штатен и не штрафуется,
  единственное следствие - в кавычках на сайте стоит только реальная цитата.
- **Ворота считает скрипт.** `gates`, `facts[].q`, `directions[].serves` и `gaps[].weight` -
  derived, агент их не проставляет.
- **Один список решений.** Что напечатано в документе 1 с кодом `d1`-`d8`, то apply-answers
  умеет принять обратно: список лежит в `_contract.mjs` и разойтись ему негде.
- **Сборка проверяет себя схемой.** `build-project.mjs` не пишет на диск контракт, который
  отвергнет `verify-data.mjs`: обход схемы у них общий.
- **Документ, который не записан, не читает никто.** Объем сверх порога - предупреждение с
  именем самого толстого раздела; отказом остаются только неподставленный маркер,
  плейсхолдер, открытый вопрос, типографика и одиннадцатый вопрос в документе 2.
- **Инвариант запретов.** В промтах и шаблонах запретов не больше, чем образцов и приемов.
  В v7 на обязательном входе писателя приходилось 125 отдельных «не» на один образец, и это
  была корневая причина плохих текстов.

Слой письма добавляет к этому шесть своих:

- **Материала больше, чем инструкций.** Потолка два и считаются они раздельно: инструкции
  12000 знаков (из них запретов 2500), материал 16000. Общего потолка не хватило бы - один
  контракт на хорошо заполненном проекте 15000 знаков. Инвариант машинный, `build-tasks.mjs`
  печатает оба счетчика в само задание и валит прогон при нарушении.
- **Страница - упорядоченные ответы на вопросы читателя.** Заголовок раздела - ОТВЕТ, а не
  ярлык секции. Вопрос стоит машиночитаемой меткой `<!-- q:<id> closes:<fNN,oN.M> -->`, и
  из нее бесплатно берутся покрытие для усиления и заготовка FAQ. Метки нет или она
  неизвестна - предупреждение и позиционный фолбэк сборки, НИКОГДА не отказ.
- **Состав страницы - арифметика, а не мнение.** Шесть правил, седьмое валит сборку на
  импорте. Первичная единица объема - число блоков; знаки лидеров лежат рядом справкой и
  целью не назначаются, потому что цель в знаках и есть машина долива воды.
- **Симметрия качества.** В v7 после писателя стояли три контролера, и все трое умели
  только РЕЗАТЬ. Теперь есть роль без этой операции: `lift.mjs` считает адресный список по
  маркерам, `site-strengthener` только добавляет, потолок +10%. Пустой список - шаг не
  вызывается вовсе: ноль токенов, нормальный исход, объяснений не требует.
- **Чужие числа ядовиты.** Форму и состав с рынка берем, цифры - никогда. Все числа лидеров
  ложатся в `tainted[]` и грепаются по собранному сайту в финале. Автору они не показываются
  вообще: место в задании дорогое, а показать запрещенное число значит потратить его на соблазн.
- **Цельность задана до письма.** `theses.md` собирается детерминированно и одинаков для всех
  авторов: в v7 страницы писались веером вслепую, и цельность потом чинилась вычитанием
  самоповторов. Канон повторяется ДОСЛОВНО.

## Раскладка проекта и порядок шагов

```
sites/NNN-<slug>/
├── queue.json            0  init и три ответа оператора
├── input/                0b вводная, при пустом входе - operator.md
├── parts/facts.json      1  site-intake: business, facts, constraints, gaps
├── parts/market.json     2  site-market: audience, offer, lexicon, competitors, scan
├── project.json          3  build-project.mjs + verify-data.mjs --seed
├── docs/understood.html  4  build-doc.mjs, затем queue.mjs docs --understood <url>
├── docs/ask.html         4  тот же запуск
└── answers.txt           5  apply-answers.mjs, затем queue.mjs gate --by "<кто>"
```

Отдельного шага разведки нет: замер лидеров снимает `site-market` на шаге 2, потому что
глубина замера зависит от тех же полей, что и сводка аудитории.

Дальше в той же папке живет слой письма:

```
sites/NNN-<slug>/
├── pages.json            1  pages.mjs, redirects.csv при переезде адреса
├── leaders/<cluster>.json 2-3 recon.mjs plan, агент leader-mapper, recon.mjs parse
├── plan.json             4  plan.mjs - состав шестью правилами
├── tasks/<slug>.md       5  build-tasks.mjs, рядом tasks/meta.json
├── theses.md             5  тезисы ВСЕХ страниц, одинаковы у всех авторов
├── pages/<slug>.md       6 и 8  site-author: страница целиком, один проход
├── lift.json, judge/     10-11 lift.mjs и site-judge по docs/v8/rubric.md
└── site.html             12 assemble.mjs, затем verify-build.mjs
```

Гейт B стоит между шагами 6 и 8: заказчику уходит ОДНА написанная страница, а не три
варианта главной в трех тонах. Спрашиваются два вопроса - манера и канон, - ответ
возвращается в `offer.tone` и `lexicon.canonical`, отметка ложится в журнал. Правка манеры
переписывает эталон, а не весь сайт.

## Что не трогается

Старый конвейер живет дальше целиком и работает как работал:

- **`/seo-faq`** и агент `faq-builder` - согласованная с SEO-сервисами машинерия;
- **`/seo-statya`** и весь конвейер статей;
- **`/seo-analiz`**, **`/seo-struktura`**, **`/seo-tekst`**, `/seo-tekst-fix`, `/seo-metategi`,
  `/seo-strategiya`, `/seo-tehaudit`, `/seo-temi`, `/seo-shablon` и все скилы раздачи;
- их агенты, скрипты и тесты: `verify-copy.mjs`, `COPY-AUDIT.md`, `VOICE.md`, `page-writer.md`,
  `.claude/tests/seo-tekst/` и остальные наборы.

Набор `.claude/tests/site/run.mjs` отдельными шагами страхует это от случайного сноса:
`/seo-faq` и `faq-builder` обязаны существовать, быть непустыми и сохранять связку с
JustMagic.

## FAQ - точка роста, а не переписывание

Текущий `/seo-faq` согласован с SEO-сервисами и работает: `jm_text_analyze`, проверка
«Акварель» через `jm_text_check`, `jm_stop_domains`, разбор task-based API с таймаутами.
Решение владельца: **FAQ остается как есть**, новые идеи для него - кандидаты на будущее, а
не повод трогать рабочую машинерию.

**Кандидат на улучшение промта `faq-builder` (не реализован, не запланирован на этап 2):**
явные нормы вхождений вместо общего указания «используй семантику». Формулировка идеи
владельца: слово из глубины входит N раз, слова из ширины входят по одному разу. Ценность в
том, что норма становится проверяемой арифметикой, а не суждением агента.

Условия, при которых этот кандидат вообще можно брать в работу:

1. нормы заданы числами, а не словами, и вычисляются из выдачи, а не назначаются;
2. проверка делается тем же прибором, что и сейчас, - без второго независимого счетчика;
3. правка идет **в промт агента**, файлы скила и его связка с сервисами не меняются.

## Что дальше: структура на контракт v8

Каталог и товары закрыты. Осталось ровно то, что названо новой границей:

- **перевод `/seo-struktura` на контракт v8.** Сейчас у структуры свой `master_list` и своя
  машинерия v7, а режим магазина строит дерево из прайса, брифа и конкурентов. Два дерева
  живут рядом и не сверяются; сводить их надо один раз и явно, а не при каждой правке;
- **письмо категории и посадки.** Пакет разработчику собран, `meta.csv` дает метатеги всех
  страниц значениями, но интро над листингом и текст под листингом пишет автор, и задания
  на них собирает обычный тракт - для `service_like` это уже работает, для `catalog` нет;
- **вилки объема** `vol.category`, `vol.facet`, `vol.product` в `plan.mjs`: без замера они
  были бы догадкой, поэтому их там нет вовсе, а не стоят нулями;
- сверка `docs/v8/rubric.md` с архивом принятых и забракованных страниц;
- строки трассировки с пометкой «этап 2» закрываются тогда же.

## Что осталось за границей этапа 2

Все четыре набора зеленые: `site` 76 из 76, `proto` 61 из 61, `catalog` 72 из 72,
`machinery` 16 из 16. Красных шагов нет ни одного, обойденных правил тоже.

### Бюджеты, выбранные в ноль

Следующий срез начинается с реза, и это граница, а не достижение:

| бюджет | занято | запас |
|---|---|---|
| блоков в `pages.yml` | 30 из 30 | нет |
| блокирующих правил `verify-page` | 12 из 12 | нет |
| предупреждающих правил `verify-page` | 8 из 12 | 4 |
| `SKILL.md` `/site proto` | 11963 из 12000 | 37 знаков |
| `CATALOG.md` режима магазина | 7650 из 9000 | 1350 знаков |
| агентов конвейера (`AGENTS_V8`) | 8 из 8 | нет, список закрыт |
| скриптов в `scripts/site` | 19 из 20 | 1 |

Отдельная строка владельцу: потолок инструкций 12000 сам по себе ограничивает
`site-author.md` плюс `AUTHOR.md` примерно девятью тысячами знаков, то есть строже, чем
объявленные раньше «`AUTHOR.md` 8000 и агент 10000». Сейчас там 2891 и 6428 при задании
около 1900, инструкции выходят 11200-11300. Два объявленных бюджета противоречат друг
другу на краях, и разводить их надо один раз и явно, а не при каждой правке промта.

### Чего в слое письма нет намеренно

- **замера и плана каталожных страниц.** `category`, `facet` и `product` типизируются из
  `catalog.json` и живут каталожным трактом; `plan.mjs` уводит их в `later` строкой «за
  границей среза», и это не заглушка, а разделение: состав категории задают данные прайса,
  а не медиана блоков у лидеров;
- **признаки ниши `catalog` и `serp_hot`** объявлены в `pages.yml`, но на состав страницы
  не влияют. Третий признак, `urgent`, на
  этапе 2 реализован: `steps` обязателен, верх вилки объема ниже на два блока;
- **восемь формул офферов и стадии прогретости из `COPY.md`.** В `AUTHOR.md` перенесены
  формула выгоды, таблица «обещайте конкретно», шесть вопросов уставшего мужика, сборка
  первого экрана и стоп-лист надписей кнопки. Выбор формулы оффера и стадия прогретости -
  это РЕШЕНИЕ о проекте, а не прием письма: их место в контракте (`offer.formula`,
  `audience.stage`), чтобы выбор делался один раз на анализе и приезжал автору материалом.
  Полем контракта это не сделано, в промт не втиснуто - названо тут и ждет владельца;
- **правка манеры по гейту B** переписывает эталон, а не весь сайт: массовой переписи
  готовых страниц в слое нет;
- **своей проверки у судьи нет.** `site-judge` выносит вердикт по рубрике и не чинит;
  вердикт в `judge/<slug>.md` читает человек.

### Что этап 2 тронул вне своих файлов

Ни один файл v7 не переписан по существу. Тронуты четыре файла, и каждый - по названной
причине:

1. `docs/MODEL-POLICY.md` - пять новых агентов внесены в таблицу с ярусом из frontmatter,
   число в заголовке поднято с 63 до 68. Без этого набор `machinery`, зеленый до этапа 2,
   становился красным: в репозитории действует инвариант «каждый агент объявляет ярус».
2. `.claude/scripts/site/_contract.mjs` (этап 1) - две АДДИТИВНЫЕ строки экспорта:
   `pageName` (имя файла страницы одним правилом на пять скриптов; без него главная и
   лендинг не находят свой план никогда) и `AGENTS_V8` (список агентов конвейера; глоб
   `site-*.md` считал бы и агентов v7). Поведение этапа 1 не изменилось.
3. `.claude/skills/site-proto/pages.yml` (этап 1) - одна строка, признак `urgent`: реестр
   обещал «hero несет телефон и срок, длинные блоки снять», а код этого не делал. Теперь
   строка описывает ровно то, что делает `plan.mjs`.
4. `.claude/tests/README.md` и этот файл - реестр наборов и карта.

### Что этап 3 тронул вне своих файлов

Режим магазина - это девять своих файлов плюс девять точечных правок в чужих, и каждая названа:

1. `.claude/scripts/site/_contract.mjs` (этап 1) - восьмое имя в `AGENTS_V8`, список закрыт.
   Агент назван `catalog-architect`, БЕЗ префикса `site-`: файлов `site-*.md` уже восемь, и
   девятый уронил бы глобовый бюджет соседнего набора.
2. `.claude/skills/site-proto/SKILL.md` (этап 2) - строки маршрутизации режима. Место под
   них освобождено, а не добавлено: убраны раскладка папки (оглавление
   `contracts-proto.md`), перечень шести маркеров усиления (он в `lift.mjs`) и перечень
   восьми правил сборки (он в `verify-build.mjs`).
3. `.claude/scripts/site/pages.mjs` (этап 2) - типизация магазина из `catalog.json` вместо
   грубого «`type shop` - значит `category`». Путь услуг не изменился ни одним шагом.
4. `.claude/scripts/site/plan.mjs` (этап 2) - каталожный тракт письма доведен: в `VOL`
   добавлены вилки `category`, `facet`, `product` и `VOL_SERVICE_LIKE`, в `TYPES_NOW` и
   `CLUSTER_OF` - три каталожных типа, `CATALOG_ONLY` стал зависеть от типа страницы
   (`catalogOnlyFor`), в `STRUCT` добавлены четыре каталожных блока, а `siteMaterial`
   берет потомков и позиции узла из страницы. Без этого 21 страница из 22 не получала ни
   плана, ни задания, ни текста.
5. `.claude/scripts/site/build-tasks.mjs` (этап 2) - блок `CATALOG_NORMS` в задание
   каталожной страницы. Целиком `CATALOG.md` писателю не подкладывается: справочник на
   8500 знаков не влезает в потолок инструкций 12000.
6. `.claude/agents/site-author.md` и `.claude/skills/site-proto/AUTHOR.md` (этап 2) -
   образец первого экрана услуги заменен образцом интро над листингом категории, в раздел
   4 вернулись два потерянных запрета (срочность только с реальной датой либо остатком,
   границы только из фактов), из промта убраны две строки, которые и так говорят задание и
   `rules.yml`. Потолок пары 12000 соблюден.
7. `.claude/skills/site-proto/pages.yml` (этап 2) - у блока `cat_text` колонка формы стала
   «H2 и 3-4 подраздела» вместо «3-6 абзацев»: норма читается там, где ее читает писатель.
8. `docs/v8/contracts-proto.md` (этап 2) - граница среза, новые поля `pages.json`
   (`from_catalog`, `kids`, `sku`) и таблица вилок `VOL`.
9. `docs/MODEL-POLICY.md` - строка `catalog-architect` с ярусом из frontmatter, число в
   заголовке поднято с 68 до 69. Без этого набор `machinery` становился красным.

## Заметки гейта 0 (2026-09-23): `/site-analiz` - единственный вход структуры и текстов

Основание - `test-text-template/docs/gate0-2026-09-23/GATE0.md`. Правки ветки `algo-v9`:

1. `pages.yml` переехал в `.claude/skills/site-analiz/`: это внутренний словарь анализа
   (`facts[].q`, вес вопросов, признаки ниши). Путь по умолчанию один на три скрипта -
   `PAGES_DEFAULT` в `_contract.mjs`. Скрипты и тесты `/site proto` по старому пути
   падают до удаления прототипа - это ожидаемо.
2. Д1 закрыт: `build-project.mjs` сохраняет `business.site` (и `null` - «сайта нет»),
   `client_pages`, `assortment`. Бюджет `project.json` поднят до 22000 предупреждения и
   32000 ошибки: IBG без этих полей весил 23900, со всеми новыми полями около 25500.
3. `parts/facts-src.json` живет весь срок задачи. `verify-data.mjs` сверяет цитату каждого
   факта дословно с `input/**` и листом ответов (нормализация е, тире, кавычек, пробелов),
   ловит служебные пометки в значении факта и висячие `objection[].facts`. Цитату со скана
   или из pdf скрипт сверить не может - это предупреждение «сверь глазами», а не отказ.
   `apply-answers.mjs` кладет цитату факта из ответа строкой листа (`answers.txt:N`).
4. Схема: `facts[].kind` (обязателен, забытый выводит сборка мостом `q -> kind`),
   `business.profile`, `business.pages_hint`, `competitors.market.page_types`,
   `audience.segments[].objection[].facts`.
5. `site-market` без полного замера многостраничника: для всех сайтов 3-5 главных лидеров
   плюс `page_types`. Разбор блоков лидеров по типам - фаза 2 `/site-tekst`.
6. `pages-planner` переписан под анализ: шаг 3b, вход только `project.json`, выход
   `structure_data.json` в формате `/seo-struktura`. Лендингу при `basic` одну главную пишет
   `build-project.mjs`, при `tier seo` состав строит `/seo-struktura`. Формат проверяет
   `verify-data.mjs` правилами `import-structure.mjs` текстов.
7. Состав сайта - решение `d9` документа 1; ответ меняет только `target_status`.
8. `AGENTS_V8` - три агента анализа: `site-intake`, `site-market`, `pages-planner`.
